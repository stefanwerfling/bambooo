import { ComponentType } from '../../Component.js';
import { Card, CardBodyType, CardLine, CardType } from '../Card/Card';
export declare class FileUpload extends Card {
    constructor(elementObject: ComponentType, bodyType?: CardBodyType, cardType?: CardType, cardLine?: CardLine);
}
